# Liveness product line (paths relative to AcasXu_closed_loop/).
# Plant physics: acas_model_params.yaml / AcasDomain.

trajectory_path: core/liveness/acas_lasso_trajectory.json
specs_path: contracts/crown/discrete/liveness/liveness_contracts.json
results_path: contracts/crown/discrete/liveness/liveness_contract_results.json

# Half-width on NN inputs (0 = exact lattice point).
nn_input_eps: 0.0

# Current ONNX dump shape (update after --dump / retrain).
expected_stem_length: 39
expected_total_states: 52

verification:
  timeout_sec: 30.0
  pgd_order: before
  device: cpu
